{
  "name": "pdf-viewer-marktext",
  "version": "1.0.0",
  "description": "PDF Viewer with Text Marking",
  "main": "index.html",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite --host 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "pdfjs-dist": "^3.11.174"
  }
} 